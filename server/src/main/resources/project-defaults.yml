
swarm:
  keycloak-server:
    providers: /var/keycloak/providers
    themes:
      defaults:
        cachethemes: false
        cachetemplates: false
        modules: example.keycloak.theme
    spis:
      event-listener:
        default-provider: example.keycloak.provider
